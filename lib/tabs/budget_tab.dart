import 'package:flutter/material.dart';

class BudgetTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Benvenuto nella pagina Budget',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
