import 'package:flutter/material.dart';

class EditTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Benvenuto nella pagina Modifica',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
