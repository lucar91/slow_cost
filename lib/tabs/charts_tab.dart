import 'package:flutter/material.dart';

class ChartsTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Benvenuto nella pagina Grafici',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
