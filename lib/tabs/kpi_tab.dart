import 'package:flutter/material.dart';

class KpiTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Benvenuto nella pagina KPI',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
